{
  "name": "db",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "generate": "pop run --env-file=.env -- npm drizzle-kit generate",
    "migrate": "op run --env-file=.env -- pnpm drizzle-kit migrate",
    "push": "op run --env-file=.env -- pnpm drizzle-kit push",
    "pull": "pop run --env-file=.env -- npm drizzle-kit pull",
    "check": "op run --env-file=.env -- pnpm drizzle-kit check",
    "up": "op run --env-file=.env -- pnpm drizzle-kit up",
    "studio": "op run --env-file=.env -- pnpm drizzle-kit studio"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.44.7",
    "postgres": "^3.4.7",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "drizzle-kit": "^0.31.6",
    "tsx": "^4.20.6"
  }
}